# A live session must not silently suspend: tor circuits die, resume
# behaviour is untested, and a suspended machine hands an attacker a frozen
# RAM image. Lid close does nothing; sleep must be a deliberate poweroff.
[Login]
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
HandleLidSwitchDocked=ignore
IdleAction=ignore
