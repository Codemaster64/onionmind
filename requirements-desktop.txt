# Keep these constraints aligned with pyproject.toml. They are repeated here so
# the isolated compiler environment does not leave editable-install metadata in
# the source tree.
PySide6-Essentials>=6.11,<6.12
requests>=2.32,<3
PySocks>=1.7,<2
Nuitka>=4.1,<5
ordered-set>=4.1,<5
zstandard>=0.23,<1
